#include "ClapTrap.hpp"

int main( void ) {
	ClapTrap a("A");
	// ClapTrap b("B");
	// ClapTrap c("C");

	// c = b = a;

	// ClapTrap d = c = b = a;
	ClapTrap e = a;

	// a.attack("B");
	// a.takeDamage(1);
	// a.takeDamage(2);
	// a.takeDamage(3);
	// a.takeDamage(3);
	// a.beRepaired(4);
	// a.takeDamage(3);
	// a.takeDamage(3);
	// a.attack("B");
	// a.takeDamage(3);
	return 0;
}
